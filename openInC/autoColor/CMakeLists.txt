cmake_minimum_required(VERSION 2.8)
project( autoColorPropject )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( autoColor autoColor.cpp )
target_link_libraries( autoColor ${OpenCV_LIBS} )
